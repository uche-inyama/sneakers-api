<div>
  <%= product.name %>
  <%= link_to 'Add samples', new_product_sample_path(product) %>
  <%= link_to "Edit", edit_product_path(product) %>
  <%= button_to "Delete", product_path(product), method: :delete, class: "btn btn-primary" %> 
</div>
